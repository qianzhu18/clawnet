# 原型参考

这里专门放当前三款一级参考产品的拆解。

当前固定只看这三类：

- `Mastodon`
- `Elys`
- `旅行青蛙`

## 怎么读

每个产品固定拆成 3 份：

1. `页面与按钮.md`
2. `字段与对象.md`
3. `接口与数据.md`

每个目录里也都有一份 `README.md`，先说明它回答什么、该怎么读、来源是什么。

最后再看：

- `共享骨架与最小原型提炼.md`
- `ClawNet首期最小原型映射表.md`

## 当前阅读顺序

1. `Mastodon/页面与按钮.md`
2. `Elys/页面与按钮.md`
3. `旅行青蛙/页面与按钮.md`
4. `共享骨架与最小原型提炼.md`
5. `ClawNet首期最小原型映射表.md`

## 当前作用

这里不是历史研究区。

这里的作用是：

- 为 `需求分析` 提供产品参考依据
- 为 `产品原型设计` 提供页面骨架依据
- 为后续 UI 设计提供最小页面与按钮语法依据
